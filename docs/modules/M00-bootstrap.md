# M00 Bootstrap

## Goal

Create the nested monorepo, env templates, compose file, scripts, and root commands.

## Current Deliverables

- Workspace root files
- Compose skeleton
- Root `make` commands
- Runtime scripts

## Gate

`docker compose config` resolves cleanly from the workspace root.
